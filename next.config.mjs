/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "https://images.unsplash.com",
      "images.unsplash.com",
      "i.imgur.com",
    ],
  },
}

export default nextConfig
